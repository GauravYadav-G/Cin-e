import SupportRequests from "@/components/support-requests";
export const metadata = { title: "Save a Support Request — CINÉ" };
export default function Contact() {
  return <SupportRequests form />;
}
